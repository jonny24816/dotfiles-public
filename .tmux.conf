source-file ~/.tmux.keys.conf
# shell options
set-option -g default-terminal 'screen-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

set -g prefix C-Space
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top       # macOS / darwin style
set -g default-terminal "${TERM}"
set -g mouse on
setw -g mode-keys vi

# catppuccin config
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_status_module_text_bg '#{E:@thm_mantle}'
set -g @catppuccin_date_time_text " %d-%m %H:%M"

set -g @catppuccin_window_text " #W"
set -g @catppuccin_window_default_text " #W"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text " #W : #{pane_current_path}"

run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

# sessionx config
set -g @sessionx-bind 'o'
set -g @sessionx-x-path '~/dotfiles-public'
set -g @sessionx-custom-paths '/Users/jonathanhughes/dotfiles-public,/Users/jonathanhughes/dotfiles'
set -g @sessionx-filter-current 'false'
set -g @sessionx-tree-mode 'on'
set -g @sessionx-window-height '85%'
set -g @sessionx-window-width '75%'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-auto-accept 'off'

# resurrect/continuum config
set -g @continuum-restore 'on'

# fingers config
set -g @fingers-pattern-0 '(jbs|ls|cd|cat|java|javac|jar|vi|zip|unzip|mv|mkdir|git|cp|touch|rm|sudo|python3|echo|find|docker|colima|man|\.  |\.\/[^\s]+\.sh|--\w+|-[a-zA-Z])(\s)(.*)$'
bind A run -b "#{@fingers-cli} start #{pane_id} --patterns 0"

# status bar config
set -g status-interval 5
set -g status-left-length 100
set -g status-right-length 100
set -g status-left '#{E:@catppuccin_status_session} '
set -g status-right '#{E:@catppuccin_status_date_time}'

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'Morantron/tmux-fingers'
set -g @plugin 'omerxx/tmux-sessionx'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
